# E2E-Audit 2026-09-08 — erster Full-Run seit der Config-Ära

`npx playwright test --project=chromium --workers=4` (34.7 min, gegen
live Production): **420 Tests — 294 passed, 125 failed, 1 flaky.**

Der alte `tests/playwright.config.js` wurde nie geladen (→ root
`playwright.config.mjs`), daher hatten die meisten Specs seit ihrem
Commit **nie einen Lauf**. Dieser Audit räumt das Erbe auf.

## Behoben (54 Failures → grün)

| Ursache                                                                                        | Files                                                                 | Fix                                                                                        |
| ---------------------------------------------------------------------------------------------- | --------------------------------------------------------------------- | ------------------------------------------------------------------------------------------ |
| `page.screenshot({path: *.webp})` — Playwright kann kein webp                                  | `screenshots.spec.js` (8), `test-molecule-studio-visual.spec.js` (36) | `.webp` → `.png`                                                                           |
| Spec erwartete `#grid` als Initial-View — App initiiert `#table` (`setActiveButton(tableBtn)`) | `test-molecule-studio-visual.spec.js` (2)                             | Test auf Initial-View Tabelle umgeschrieben; Visual-Style-Test klickt jetzt `#grid` vorher |

## Quarantäne (22 Tests → tests/e2e/quarantine/)

Specs für Features, die (noch) nicht live sind — siehe
`quarantine/README.md`: `interactive-experiments` (18), `curricula-ux`
(4, ersetzt durch `curricula-dropdown.spec.js`), `test-dark-mode` (7,
altes Theme-UI), `test-titrations-simulator` (7, Platzhalter-Seite).

## Bekannt flaky (absorbiert durch retries: 2)

- `sw-freshness.spec.js` „Live-sw.js serviert exakt die Repo-Version“ —
  CDN-Timing beim ersten Fetch; Retry grün. Versionen matchen (v9-2026-08).
- `test-formula-rendering.spec.js` KaTeX-Content (1× im Audit-Lauf).

## Verbleibendes Wartungs-Backlog (~47 Failures, 10 Files)

**Nicht in Quarantäne** — diese Specs testen EXISTIERENDE Features
(Seiten live, `calculator-container` vorhanden, JS rendert dynamisch).
Die Failures sind Selektor-Drift im dynamischen DOM (JS-gerenderte
IDs/Buttons) und brauchen echtes Browser-Debugging pro Test:

| File                                                   | failed           | Fehlerbild (Audit)                                                     |
| ------------------------------------------------------ | ---------------- | ---------------------------------------------------------------------- |
| `test-calculators.spec.js`                             | 8                | toBeVisible/toBeTruthy — Rechner-UI-Selektoren                         |
| `test-titrations-simulator.spec.js`                    | siehe Quarantäne | —                                                                      |
| `test-molekuel-studio.spec.js`                         | 7                | Shortcuts/Zoom-Selektoren                                              |
| `test-verbrennungsrechner.spec.js`                     | 6                | fuel-input/result-Selektoren                                           |
| `test-gasgesetz-rechner.spec.js`                       | 6                | result/formula-Selektoren, strict-mode-violation (mehrere Temp-Inputs) |
| `test-visualizations.spec.js`                          | 4                | Einzelfälle in 34                                                      |
| `test-pages.features.spec.js`                          | 4                | Einzelfälle in 58                                                      |
| `test-formula-rendering.spec.js`                       | 3                | Bild-Selektoren                                                        |
| `test-language-switcher.spec.js`                       | 2                | Umschalt-Verhalten                                                     |
| `test-curricula-modulhandbuch.spec.js`                 | 2                | alte UX-Erwartungen (Graph auf Index, Topic-Cards BY)                  |
| Singles (quiz, pwa, progress, pse-integration, entity) | je 1             | Einzelfälle                                                            |

**Ansatz pro File**: Seite lokal mit `BASE_URL` + `--headed` öffnen,
erwarteten Selektor gegen das echte gerenderte DOM prüfen, Selektor
fixen oder `test.fixme(..., 'TODO: Feature/Selektor-Drift — AUDIT-2026-09-08')`
setzen. Faustregel aus diesem Audit: Erst statisch verifizieren
(`curl … | grep`), dann Browser-Debug — die Hälfte der Failures war
statisch erkennbar (webp, fehlende Container).

## Suite-Zustand nach Audit

- Default (`npx playwright test`): 392 Tests / 23 Files
- Mit Quarantäne (`E2E_INCLUDE_QUARANTINE=1`): 420 / 25
- Grün seit Audit: curricula-dropdown 29/29, molecule-studio-visual
  44/44, screenshots 8/8, sw-freshness (flaky-safe), rest siehe oben
