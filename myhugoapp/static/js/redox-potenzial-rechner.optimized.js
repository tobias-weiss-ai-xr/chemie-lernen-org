 const FARADAY_CONSTANT = 96485;const GAS_CONSTANT = 8.314;const STANDARD_TEMPERATURE = 298.15;function parseNumber(value){value = value.trim();const num = parseFloat(value);if (isNaN(num)){throw new Error('Ungültige Zahl: ' + value)} return num} function formatNumber(value,decimals = 3){if (Math.abs(value) < 0.001 || Math.abs(value) >= 10000){return value.toExponential(decimals - 1)} return value.toFixed(decimals)} function calculateCellPotential(){const cathodeInput = document.getElementById('cathode-potential').value.trim();const anodeInput = document.getElementById('anode-potential').value.trim();const electronsInput = document.getElementById('electrons').value.trim();if (!cathodeInput || !anodeInput || !electronsInput){showError('Bitte füllen Sie alle Felder aus.');return} try{const cathodePotential = parseNumber(cathodeInput);const anodePotential = parseNumber(anodeInput);const n = parseInt(electronsInput);if (n < 1 || n > 10){showError('Anzahl der Elektronen muss zwischen 1 und 10 liegen.');return} const cellPotential = cathodePotential - anodePotential;const deltaG = -n * FARADAY_CONSTANT * cellPotential;const k = Math.exp((n * FARADAY_CONSTANT * cellPotential) / (GAS_CONSTANT * STANDARD_TEMPERATURE));let spontaneity,spontaneityClass;if (cellPotential > 0.001){spontaneity = 'Spontan (exergonisch)';spontaneityClass = 'success'} else if (cellPotential < -0.001){spontaneity = 'Nicht spontan (endergonisch)';spontaneityClass = 'danger'} else{spontaneity = 'Gleichgewicht';spontaneityClass = 'warning'} displayCellPotentialResults(cathodePotential,anodePotential,cellPotential,n,deltaG,k,spontaneity,spontaneityClass)} catch (error){showError(error.message)} } function calculateNernstPotential(){const cathodeInput = document.getElementById('nernst-cathode').value.trim();const anodeInput = document.getElementById('nernst-anode').value.trim();const nInput = document.getElementById('nernst-electrons').value.trim();const tempInput = document.getElementById('temperature').value.trim();const oxidizedInput = document.getElementById('oxidized-conc').value.trim();const reducedInput = document.getElementById('reduced-conc').value.trim();if (!cathodeInput || !anodeInput || !nInput || !tempInput || !oxidizedInput || !reducedInput){showError('Bitte füllen Sie alle Felder aus.');return} try{const cathodePotential = parseNumber(cathodeInput);const anodePotential = parseNumber(anodeInput);const n = parseInt(nInput);const T = parseNumber(tempInput);const [ox] = parseNumber(oxidizedInput);const [red] = parseNumber(reducedInput);if (n < 1 || n > 10){showError('Anzahl der Elektronen muss zwischen 1 und 10 liegen.');return} const e0 = cathodePotential - anodePotential;const Q = ox / red;const nernstTerm = (GAS_CONSTANT * T) / (n * FARADAY_CONSTANT) * Math.log(Q);const eCell = e0 - nernstTerm;const log10Term = (0.0592 / n) * Math.log10(Q);const eCellLog10 = e0 - log10Term;displayNernstResults(e0,n,T,ox,red,Q,nernstTerm,eCell,log10Term,eCellLog10)} catch (error){showError(error.message)} } function calculateGibbsEnergy(){const eCellInput = document.getElementById('cell-potential').value.trim();const nInput = document.getElementById('gibbs-electrons').value.trim();const tempInput = document.getElementById('gibbs-temp').value.trim();if (!eCellInput || !nInput || !tempInput){showError('Bitte füllen Sie alle Felder aus.');return} try{const eCell = parseNumber(eCellInput);const n = parseInt(nInput);const T = parseNumber(tempInput);if (n < 1 || n > 10){showError('Anzahl der Elektronen muss zwischen 1 und 10 liegen.');return} const deltaG = -n * FARADAY_CONSTANT * eCell;const deltaGKJ = deltaG / 1000;const k = Math.exp((n * FARADAY_CONSTANT * eCell) / (GAS_CONSTANT * T));let spontaneity,spontaneityClass;if (deltaG < -100){spontaneity = 'Spontan (exergonisch)';spontaneityClass = 'success'} else if (deltaG > 100){spontaneity = 'Nicht spontan (endergonisch)';spontaneityClass = 'danger'} else{spontaneity = 'Gleichgewicht';spontaneityClass = 'warning'} displayGibbsResults(eCell,n,T,deltaG,deltaGKJ,k,spontaneity,spontaneityClass)} catch (error){showError(error.message)} } function displayCellPotentialResults(cathodeE,anodeE,cellE,n,deltaG,k,spontaneity,spontaneityClass){const mainResult = document.getElementById('main-result');const additionalResult = document.getElementById('additional-result');const details = document.getElementById('calculation-details');const title = document.getElementById('result-title');const additionalTitle = document.getElementById('additional-title');title.textContent = 'Zellpotential';additionalTitle.textContent = 'Energetik & Gleichgewicht';mainResult.innerHTML = ` <div class="result-value-large"> <span class="result-number ${cellE > 0 ? 'positive' : cellE < 0 ? 'negative' : 'neutral'}"> ${formatNumber(cellE)} V </span> </div> <p class="result-label">Standard-Zellpotential E°zelle</p> <div class="spontaneity-indicator ${spontaneityClass}"> <i class="fa fa-${spontaneityClass === 'success' ? 'check' : spontaneityClass === 'danger' ? 'times' : 'minus'}-circle"></i> ${spontaneity} </div> `;additionalResult.innerHTML = ` <div class="additional-item"> <span class="additional-label">Gibbs-Energie (ΔG°):</span> <span class="additional-value">${formatNumber(deltaG / 1000)} kJ/mol</span> </div> <div class="additional-item"> <span class="additional-label">Gleichgewichtskonstante (K):</span> <span class="additional-value">${formatKValue(k)}</span> </div> `;details.innerHTML = ` <div class="calculation-step"> <h4>Gegebene Potentiale:</h4> <p class="formula">E°(Kathode) = ${formatNumber(cathodeE)} V</p> <p class="formula">E°(Anode) = ${formatNumber(anodeE)} V</p> <p class="formula">n = ${n} Elektronen</p> </div> <div class="calculation-step"> <h4>Zellpotential berechnen:</h4> <p class="formula">E°zelle = E°kathode - E°anode</p> <p class="formula">E°zelle = ${formatNumber(cathodeE)} V - (${formatNumber(anodeE)} V)</p> <p class="formula">E°zelle = ${formatNumber(cellE)} V</p> </div> <div class="calculation-step"> <h4>Gibbs-Energie (ΔG°):</h4> <p class="formula">ΔG° = -n · F · E°zelle</p> <p class="formula">ΔG° = -${n} · 96485 C/mol · ${formatNumber(cellE)} V</p> <p class="formula">ΔG° = ${formatNumber(deltaG / 1000)} kJ/mol</p> </div> <div class="calculation-step"> <h4>Gleichgewichtskonstante (K):</h4> <p class="formula">K = exp(n · F · E°zelle / R · T)</p> <p class="formula">K = exp(${n} · 96485 · ${formatNumber(cellE)} / 8.314 · ${STANDARD_TEMPERATURE})</p> <p class="formula">K = ${formatKValue(k)}</p> </div> <div class="calculation-step"> <h4>Spontanität:</h4> <p>${spontaneity}</p> <p class="formula">E°zelle ${cellE > 0 ? '>' : cellE < 0 ? '<' : '='} 0 → ${spontaneity.toLowerCase()}</p> </div> `;showResults()} function displayNernstResults(e0,n,T,ox,red,Q,nernstTerm,eCell,log10Term,eCellLog10){const mainResult = document.getElementById('main-result');const additionalResult = document.getElementById('additional-result');const details = document.getElementById('calculation-details');const title = document.getElementById('result-title');const additionalTitle = document.getElementById('additional-title');title.textContent = 'Zellpotential (Nernst)';additionalTitle.textContent = 'Nernst-Terme';mainResult.innerHTML = ` <div class="result-value-large"> <span class="result-number ${eCell > 0 ? 'positive' : eCell < 0 ? 'negative' : 'neutral'}"> ${formatNumber(eCell)} V </span> </div> <p class="result-label">Zellpotential unter Nicht-Standard-Bedingungen</p> `;additionalResult.innerHTML = ` <div class="additional-item"> <span class="additional-label">Standard E°zelle:</span> <span class="additional-value">${formatNumber(e0)} V</span> </div> <div class="additional-item"> <span class="additional-label">Reaktionsquotient (Q):</span> <span class="additional-value">${formatNumber(Q)}</span> </div> <div class="additional-item"> <span class="additional-label">Nernst-Korrektur:</span> <span class="additional-value">${formatNumber(nernstTerm * 1000)} mV</span> </div> ${Math.abs(eCell - eCellLog10) > 0.001 ? ` <div class="additional-item"> <span class="additional-label">E (log₁₀-Form):</span> <span class="additional-value">${formatNumber(eCellLog10)} V</span> </div> ` : ''} `;details.innerHTML = ` <div class="calculation-step"> <h4>Gegebene Werte:</h4> <p class="formula">E°zelle = ${formatNumber(e0)} V</p> <p class="formula">n = ${n} Elektronen</p> <p class="formula">T = ${formatNumber(T)} K</p> <p class="formula">[ox] = ${formatNumber(ox)} mol/L</p> <p class="formula">[red] = ${formatNumber(red)} mol/L</p> </div> <div class="calculation-step"> <h4>Reaktionsquotient:</h4> <p class="formula">Q = [ox]/[red]</p> <p class="formula">Q = ${formatNumber(ox)}/${formatNumber(red)}</p> <p class="formula">Q = ${formatNumber(Q)}</p> </div> <div class="calculation-step"> <h4>Nernst-Gleichung (natürlicher Logarithmus):</h4> <p class="formula">E = E° - (RT/nF) · ln(Q)</p> <p class="formula">E = ${formatNumber(e0)} - (${formatNumber(GAS_CONSTANT * T / (n * FARADAY_CONSTANT))}) · ln(${formatNumber(Q)})</p> <p class="formula">E = ${formatNumber(e0)} - ${formatNumber(nernstTerm * 1000)} mV</p> <p class="formula">E = ${formatNumber(eCell)} V</p> </div> <div class="calculation-step"> <h4>Nernst-Gleichung (log₁₀ bei 25°C):</h4> <p class="formula">E = E° - (0.0592 V/n) · log₁₀(Q)</p> <p class="formula">E = ${formatNumber(e0)} - (0.0592/${n}) · log₁₀(${formatNumber(Q)})</p> <p class="formula">E = ${formatNumber(e0)} - ${formatNumber(log10Term * 1000)} mV</p> <p class="formula">E = ${formatNumber(eCellLog10)} V</p> </div> `;showResults()} function displayGibbsResults(eCell,n,T,deltaG,deltaGKJ,k,spontaneity,spontaneityClass){const mainResult = document.getElementById('main-result');const additionalResult = document.getElementById('additional-result');const details = document.getElementById('calculation-details');const title = document.getElementById('result-title');const additionalTitle = document.getElementById('additional-title');title.textContent = 'Gibbs-Energie';additionalTitle.textContent = 'Gleichgewichtskonstante';mainResult.innerHTML = ` <div class="result-value-large"> <span class="result-number ${deltaG < 0 ? 'negative' : deltaG > 0 ? 'positive' : 'neutral'}"> ${formatNumber(deltaGKJ)} kJ/mol </span> </div> <p class="result-label">Gibbs-Energie (ΔG°)</p> <div class="spontaneity-indicator ${spontaneityClass}"> <i class="fa fa-${spontaneityClass === 'success' ? 'check' : spontaneityClass === 'danger' ? 'times' : 'minus'}-circle"></i> ${spontaneity} </div> `;additionalResult.innerHTML = ` <div class="additional-item"> <span class="additional-label">Zellpotential E°:</span> <span class="additional-value">${formatNumber(eCell)} V</span> </div> <div class="additional-item"> <span class="additional-label">Gleichgewichtskonstante (K):</span> <span class="additional-value">${formatKValue(k)}</span> </div> ${k > 1e10 ? '<p class="k-note"><small>⚠️ Sehr großes K: Reaktion läuft praktisch vollständig ab</small></p>' : ''} ${k < 1e-10 ? '<p class="k-note"><small>⚠️ Sehr kleines K: Reaktion läuft praktisch nicht ab</small></p>' : ''} `;details.innerHTML = ` <div class="calculation-step"> <h4>Gegebene Werte:</h4> <p class="formula">E°zelle = ${formatNumber(eCell)} V</p> <p class="formula">n = ${n} Elektronen</p> <p class="formula">T = ${formatNumber(T)} K</p> </div> <div class="calculation-step"> <h4>Gibbs-Energie berechnen:</h4> <p class="formula">ΔG° = -n · F · E°zelle</p> <p class="formula">ΔG° = -${n} · 96485 C/mol · ${formatNumber(eCell)} V</p> <p class="formula">ΔG° = ${formatNumber(deltaG)} J/mol</p> <p class="formula">ΔG° = ${formatNumber(deltaGKJ)} kJ/mol</p> </div> <div class="calculation-step"> <h4>Gleichgewichtskonstante berechnen:</h4> <p class="formula">K = exp(-ΔG°/RT) = exp(n · F · E°zelle / R · T)</p> <p class="formula">K = exp(${n} · 96485 · ${formatNumber(eCell)} / 8.314 · ${formatNumber(T)})</p> <p class="formula">K = exp(${formatNumber((n * FARADAY_CONSTANT * eCell) / (GAS_CONSTANT * T))})</p> <p class="formula">K = ${formatKValue(k)}</p> </div> <div class="calculation-step"> <h4>Spontanität:</h4> <p>${spontaneity}</p> <p class="formula">ΔG° ${deltaG < -100 ? '<' : deltaG > 100 ? '>' : '≈'} 0 → ${spontaneity.toLowerCase()}</p> <p class="formula">E°zelle ${eCell > 0 ? '>' : eCell < 0 ? '<' : '='} 0 → ${spontaneity.toLowerCase()}</p> </div> `;showResults()} function formatKValue(k){if (k === 0) return '0';if (k === Infinity) return '∞';if (!isFinite(k)) return 'N/A';if (k < 1e-10){return k.toExponential(2)} else if (k > 1e10){return k.toExponential(2)} else if (k < 0.01 || k > 1000){return k.toExponential(3)} else{return k.toFixed(3)} } function showResults(){document.getElementById('results-section').style.display = 'block';document.getElementById('error-section').style.display = 'none';document.getElementById('results-section').scrollIntoView({behavior: 'smooth',block: 'start' })} function showError(message){document.getElementById('error-message').textContent = message;document.getElementById('error-section').style.display = 'block';document.getElementById('results-section').style.display = 'none'} document.addEventListener('DOMContentLoaded',function(){document.querySelectorAll('.example-btn').forEach(button =>{button.addEventListener('click',function(){document.getElementById('cathode-potential').value = this.dataset.cathode;document.getElementById('anode-potential').value = this.dataset.anode;document.getElementById('electrons').value = this.dataset.electrons})});document.querySelectorAll('.example-btn-nernst').forEach(button =>{button.addEventListener('click',function(){document.getElementById('nernst-cathode').value = this.dataset.cathode;document.getElementById('nernst-anode').value = this.dataset.anode;document.getElementById('nernst-electrons').value = this.dataset.n;document.getElementById('temperature').value = this.dataset.temp;document.getElementById('oxidized-conc').value = this.dataset.ox;document.getElementById('reduced-conc').value = this.dataset.red})});document.querySelectorAll('.example-btn-gibbs').forEach(button =>{button.addEventListener('click',function(){document.getElementById('cell-potential').value = this.dataset.e;document.getElementById('gibbs-electrons').value = this.dataset.n;document.getElementById('gibbs-temp').value = this.dataset.temp})});const inputs = document.querySelectorAll('.form-control');inputs.forEach(input =>{input.addEventListener('keypress',function(e){if (e.key === 'Enter'){const section = this.closest('.tab-pane');if (section.id === 'standard-cell'){calculateCellPotential()} else if (section.id === 'nernst'){calculateNernstPotential()} else if (section.id === 'gibbs'){calculateGibbsEnergy()} } })})});