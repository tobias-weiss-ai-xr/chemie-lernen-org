 class ChemistryCalculator{constructor(config){this.config ={title: config.title || 'Chemie Rechner',inputFields: config.inputFields || [],resultFields: config.resultFields || [],calculations: config.calculations ||{},validation: config.validation ||{},units: config.units ||{} };this.elements ={};this.results ={};this.init()} init(){this.config.inputFields.forEach(field =>{const element = document.getElementById(field.id);if (element){this.elements[field.id] = element;element.addEventListener('input',(e) => this.validateInput(field.id,e.target.value));element.addEventListener('keypress',(e) =>{if (e.key === 'Enter'){this.calculate()} })} });this.config.resultFields.forEach(field =>{const element = document.getElementById(field.id);if (element){this.results[field.id] = element} });const calculateBtn = document.querySelector('.calculate-btn');if (calculateBtn){calculateBtn.addEventListener('click',() => this.calculate())} } validateInput(fieldId,value){const fieldConfig = this.config.inputFields.find(f => f.id === fieldId);if (!fieldConfig) return true;const validation = this.config.validation[fieldId];if (!validation) return true;if (validation.type === 'number'){const numValue = parseFloat(value);if (isNaN(numValue)){this.showValidationError(fieldId,'Bitte geben Sie eine gültige Zahl ein.');return false} if (validation.min !== undefined && numValue < validation.min){this.showValidationError(fieldId,`Wert muss größer als ${validation.min} sein.`);return false} if (validation.max !== undefined && numValue > validation.max){this.showValidationError(fieldId,`Wert muss kleiner als ${validation.max} sein.`);return false} } if (typeof validation.validate === 'function'){const isValid = validation.validate(value);if (!isValid){this.showValidationError(fieldId,validation.errorMessage || 'Ungültige Eingabe.');return false} } if (validation.pattern && !new RegExp(validation.pattern).test(value)){this.showValidationError(fieldId,validation.errorMessage || 'Ungültiges Format.');return false} this.clearValidationError(fieldId);return true} showValidationError(fieldId,message){const field = this.elements[fieldId];if (field){field.classList.add('error');let errorContainer = field.parentNode.querySelector('.error-message');if (!errorContainer){errorContainer = document.createElement('div');errorContainer.className = 'error-message';field.parentNode.appendChild(errorContainer)} errorContainer.textContent = message;errorContainer.style.display = 'block';setTimeout(() =>{errorContainer.style.display = 'none';field.classList.remove('error')},5000)} } clearValidationError(fieldId){const field = this.elements[fieldId];if (field){field.classList.remove('error');const errorContainer = field.parentNode.querySelector('.error-message');if (errorContainer){errorContainer.style.display = 'none'} } } validateAllFields(){for (const fieldConfig of this.config.inputFields){const element = this.elements[fieldConfig.id];const value = element ? element.value : '';if (!this.validateInput(fieldConfig.id,value)){return false} } return true} calculate(){if (!this.validateAllFields()){return} const inputs ={};for (const fieldConfig of this.config.inputFields){const element = this.elements[fieldConfig.id];if (element){inputs[fieldConfig.id] = this.parseValue(element.value,fieldConfig.type)} } try{const results = this.config.calculations.calculate(inputs);this.displayResults(results);for (const fieldId of Object.keys(this.elements)){this.clearValidationError(fieldId)} } catch (error){const errorContainer = document.querySelector('.calculator-error') || this.createErrorContainer();errorContainer.textContent = `Berechnungsfehler: ${error.message}`;errorContainer.style.display = 'block';console.error('Calculation error:',error)} } parseValue(value,type){switch (type){case 'number': return parseFloat(value) || 0;case 'select': return value;case 'checkbox': return value === 'on' || value === 'true';case 'text': default: return value.toString().trim()} } displayResults(results){for (const [fieldId,value] of Object.entries(results)){const resultElement = this.results[fieldId];if (resultElement){resultElement.textContent = value;resultElement.style.display = 'block';resultElement.classList.add('result-updated');setTimeout(() =>{resultElement.classList.remove('result-updated')},300)} } } createErrorContainer(){const container = document.createElement('div');container.className = 'calculator-error';container.style.display = 'none';container.style.padding = '10px';container.style.marginBottom = '10px';container.style.border = '1px solid #dc3545';container.style.borderRadius = '4px';container.style.backgroundColor = '#f8d7da';container.style.color = '#721c24';return container} } window.ChemistryCalculator = ChemistryCalculator;