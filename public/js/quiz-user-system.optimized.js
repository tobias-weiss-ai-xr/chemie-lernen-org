 class QuizUserSystem{constructor(options ={}){this.storageKey = options.storageKey || 'chemie-lernen-users';this.currentUser = null;this.users = this.loadUsers();this.highScores = this.loadHighScores()} loadUsers(){try{const data = localStorage.getItem(this.storageKey);return data ? JSON.parse(data) :{}} catch (e){console.error('Error loading users:',e);return{}} } saveUsers(){try{localStorage.setItem(this.storageKey,JSON.stringify(this.users))} catch (e){console.error('Error saving users:',e)} } loadHighScores(){try{const data = localStorage.getItem(this.storageKey + '-highscores');return data ? JSON.parse(data) : []} catch (e){console.error('Error loading high scores:',e);return []} } saveHighScores(){try{localStorage.setItem(this.storageKey + '-highscores',JSON.stringify(this.highScores))} catch (e){console.error('Error saving high scores:',e)} } register(username,password,displayName = null){if (!username || username.length < 3){return{success: false,message: 'Benutzername muss mindestens 3 Zeichen lang sein' }} if (this.users[username]){return{success: false,message: 'Benutzername bereits vergeben' }} this.users[username] ={username: username,password: this.hashPassword(password),displayName: displayName || username,createdAt: new Date().toISOString(),lastLogin: null,totalScore: 0,quizzesCompleted: 0,achievements: [] };this.saveUsers();return{success: true,message: 'Registrierung erfolgreich!' }} login(username,password){const user = this.users[username];if (!user){return{success: false,message: 'Benutzer nicht gefunden' }} if (user.password !== this.hashPassword(password)){return{success: false,message: 'Falsches Passwort' }} this.currentUser = user;user.lastLogin = new Date().toISOString();this.saveUsers();window.dispatchEvent(new CustomEvent('userLogin',{detail:{username: user.username,displayName: user.displayName } }));return{success: true,message: 'Erfolgreich eingeloggt!' }} logout(){if (this.currentUser){const username = this.currentUser.username;this.currentUser = null;window.dispatchEvent(new CustomEvent('userLogout',{detail:{username: username } }));return{success: true,message: 'Erfolgreich ausgeloggt!' }} return{success: false,message: 'Kein Benutzer eingeloggt' }} getCurrentUser(){return this.currentUser} isLoggedIn(){return this.currentUser !== null} updateProfile(displayName){if (!this.currentUser){return{success: false,message: 'Nicht eingeloggt' }} this.currentUser.displayName = displayName;this.users[this.currentUser.username] = this.currentUser;this.saveUsers();return{success: true,message: 'Profil aktualisiert!' }} hashPassword(password){let hash = 0;for (let i = 0;i < password.length;i++){const char = password.charCodeAt(i);hash = ((hash << 5) - hash) + char;hash = hash & hash} return hash.toString(16)} submitScore(topicId,score,percentage,timeSpent = null,hintsUsed = 0){if (!this.currentUser){return{success: false,message: 'Nicht eingeloggt' }} const scoreEntry ={id: Date.now().toString() + Math.random().toString(36).substr(2,9),username: this.currentUser.username,displayName: this.currentUser.displayName,topicId: topicId,score: score,percentage: percentage,timeSpent: timeSpent,hintsUsed: hintsUsed,timestamp: new Date().toISOString() };this.highScores.push(scoreEntry);this.highScores.sort((a,b) =>{if (b.percentage !== a.percentage){return b.percentage - a.percentage} if (a.timeSpent && b.timeSpent && a.timeSpent !== b.timeSpent){return a.timeSpent - b.timeSpent} return a.hintsUsed - b.hintsUsed});const topicScores = this.highScores.filter(s => s.topicId === topicId);const otherScores = this.highScores.filter(s => s.topicId !== topicId);this.highScores = [ ...otherScores,...topicScores.slice(0,100) ];this.saveHighScores();this.currentUser.totalScore += score;this.currentUser.quizzesCompleted++;this.checkAchievements();this.saveUsers();return{success: true,message: 'Ergebnis gespeichert!',rank: this.getRank(topicId,scoreEntry) }} getRank(topicId,scoreEntry){const topicScores = this.highScores .filter(s => s.topicId === topicId) .sort((a,b) =>{if (b.percentage !== a.percentage) return b.percentage - a.percentage;if (a.timeSpent && b.timeSpent && a.timeSpent !== b.timeSpent) return a.timeSpent - b.timeSpent;return a.hintsUsed - b.hintsUsed});return topicScores.findIndex(s => s.id === scoreEntry.id) + 1} getHighScores(topicId,limit = 10){return this.highScores .filter(s => s.topicId === topicId) .slice(0,limit)} getUserScores(username = null){const user = username || this.currentUser?.username;if (!user) return [];return this.highScores.filter(s => s.username === user)} getLeaderboard(topicId = null,limit = 10){let scores = topicId ? this.highScores.filter(s => s.topicId === topicId) : this.highScores;const userTotals ={};scores.forEach(score =>{if (!userTotals[score.username]){userTotals[score.username] ={username: score.username,displayName: score.displayName,totalScore: 0,quizzesCompleted: 0,averagePercentage: 0,totalPercentage: 0 }} userTotals[score.username].totalScore += score.score;userTotals[score.username].quizzesCompleted++;userTotals[score.username].totalPercentage += score.percentage});Object.values(userTotals).forEach(user =>{user.averagePercentage = Math.round(user.totalPercentage / user.quizzesCompleted)});return Object.values(userTotals) .sort((a,b) => b.totalScore - a.totalScore) .slice(0,limit)} checkAchievements(){if (!this.currentUser) return;const achievements = this.currentUser.achievements || [];const newAchievements = [];if (this.currentUser.quizzesCompleted === 1 && !achievements.includes('first-quiz')){newAchievements.push({id: 'first-quiz',title: 'Erste Schritte',description: 'Erstes Quiz abgeschlossen',icon: '🎯' })} if (this.currentUser.quizzesCompleted >= 10 && !achievements.includes('quiz-master')){newAchievements.push({id: 'quiz-master',title: 'Quiz-Meister',description: '10 Quizze abgeschlossen',icon: '🏆' })} const recentPerfectScores = this.getUserScores() .filter(s => s.percentage === 100) .length;if (recentPerfectScores > 0 && !achievements.includes('perfect-score')){newAchievements.push({id: 'perfect-score',title: 'Perfektionist',description: 'Ein Quiz mit 100% abgeschlossen',icon: '💯' })} const fastScores = this.getUserScores() .filter(s => s.timeSpent && s.timeSpent < 300 && s.percentage >= 80);if (fastScores.length > 0 && !achievements.includes('speed-demon')){newAchievements.push({id: 'speed-demon',title: 'Geschwindigkeitsdämon',description: 'Quiz in unter 5 Minuten mit 80%+ abgeschlossen',icon: '⚡' })} const noHintsScores = this.getUserScores() .filter(s => s.hintsUsed === 0 && s.percentage >= 90);if (noHintsScores.length > 0 && !achievements.includes('no-hints')){newAchievements.push({id: 'no-hints',title: 'Alleinunternehmer',description: 'Quiz ohne Hinweise mit 90%+ abgeschlossen',icon: '🧠' })} if (newAchievements.length > 0){this.currentUser.achievements = [ ...achievements,...newAchievements.map(a => a.id) ];window.dispatchEvent(new CustomEvent('achievementsUnlocked',{detail:{achievements: newAchievements } }))} } getAchievements(username = null){const user = username || this.currentUser?.username;if (!user) return [];const allAchievements ={'first-quiz':{title: 'Erste Schritte',description: 'Erstes Quiz abgeschlossen',icon: '🎯' },'quiz-master':{title: 'Quiz-Meister',description: '10 Quizze abgeschlossen',icon: '🏆' },'perfect-score':{title: 'Perfektionist',description: 'Ein Quiz mit 100% abgeschlossen',icon: '💯' },'speed-demon':{title: 'Geschwindigkeitsdämon',description: 'Quiz in unter 5 Minuten mit 80%+ abgeschlossen',icon: '⚡' },'no-hints':{title: 'Alleinunternehmer',description: 'Quiz ohne Hinweise mit 90%+ abgeschlossen',icon: '🧠' } };const userData = this.users[user];if (!userData) return [];return (userData.achievements || []).map(id => allAchievements[id]).filter(Boolean)} deleteAccount(){if (!this.currentUser){return{success: false,message: 'Nicht eingeloggt' }} const username = this.currentUser.username;delete this.users[username];this.highScores = this.highScores.filter(s => s.username !== username);this.saveUsers();this.saveHighScores();this.currentUser = null;window.dispatchEvent(new CustomEvent('userAccountDeleted',{detail:{username: username } }));return{success: true,message: 'Konto gelöscht!' }} } const quizUserSystem = new QuizUserSystem({storageKey: 'chemie-lernen-users' });if (typeof module !== 'undefined' && module.exports){module.exports ={QuizUserSystem,quizUserSystem }} 