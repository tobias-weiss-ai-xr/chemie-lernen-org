 class QuizSystem{constructor(options ={}){this.storageKey = options.storageKey || 'chemie-lernen-quiz-progress';this.quizzes ={};this.progress = this.loadProgress();this.currentQuiz = null;this.currentQuestionIndex = 0;this.score = 0;this.answers = [];this.timer = null;this.timeRemaining = 0;this.hintsUsed = 0;this.quizSettings ={timedMode: false,timePerQuestion: null,totalTime: null,allowHints: true,randomizeQuestions: false,showExplanations: true }} registerQuiz(topicId,quizData){this.quizzes[topicId] ={id: topicId,title: quizData.title,questions: quizData.questions,passingScore: quizData.passingScore || 70,defaultTimePerQuestion: quizData.timePerQuestion || null,defaultTotalTime: quizData.totalTime || null }} configureQuiz(settings ={}){this.quizSettings ={timedMode: settings.timedMode !== undefined ? settings.timedMode : this.quizSettings.timedMode,timePerQuestion: settings.timePerQuestion || this.quizSettings.timePerQuestion,totalTime: settings.totalTime || this.quizSettings.totalTime,allowHints: settings.allowHints !== undefined ? settings.allowHints : this.quizSettings.allowHints,randomizeQuestions: settings.randomizeQuestions !== undefined ? settings.randomizeQuestions : this.quizSettings.randomizeQuestions,showExplanations: settings.showExplanations !== undefined ? settings.showExplanations : this.quizSettings.showExplanations }} shuffleArray(array){const shuffled = [...array];for (let i = shuffled.length - 1;i > 0;i--){const j = Math.floor(Math.random() * (i + 1));[shuffled[i],shuffled[j]] = [shuffled[j],shuffled[i]]} return shuffled} startQuiz(topicId,settings ={}){const quiz = this.quizzes[topicId];if (!quiz){console.error(`Quiz for topic ${topicId} not found`);return false} this.configureQuiz(settings);const questions = this.quizSettings.randomizeQuestions ? this.shuffleArray(quiz.questions) : quiz.questions;this.currentQuiz ={...quiz,questions: questions };this.currentQuestionIndex = 0;this.score = 0;this.answers = [];this.hintsUsed = 0;if (this.quizSettings.timedMode){const timeLimit = this.quizSettings.totalTime || this.quizSettings.timePerQuestion;if (timeLimit){this.timeRemaining = timeLimit;this.startTimer()} } return true} startTimer(){if (this.timer){clearInterval(this.timer)} this.timer = setInterval(() =>{this.timeRemaining--;window.dispatchEvent(new CustomEvent('quizTimerUpdate',{detail:{timeRemaining: this.timeRemaining } }));if (this.timeRemaining <= 0){this.stopTimer();window.dispatchEvent(new CustomEvent('quizTimeUp',{}))} },1000)} stopTimer(){if (this.timer){clearInterval(this.timer);this.timer = null} } getTimeRemaining(){return this.timeRemaining} getFormattedTime(seconds){const mins = Math.floor(seconds / 60);const secs = seconds % 60;return `${mins}:${secs.toString().padStart(2,'0')}`} getHint(){if (!this.quizSettings.allowHints){return{success: false,message: 'Hints are not enabled for this quiz' }} const question = this.getCurrentQuestion();if (!question){return{success: false,message: 'No current question' }} if (!question.hint){return{success: false,message: 'No hint available for this question' }} this.hintsUsed++;return{success: true,hint: question.hint,hintsUsed: this.hintsUsed }} getCurrentQuestion(){if (!this.currentQuiz) return null;return this.currentQuiz.questions[this.currentQuestionIndex]} submitAnswer(answer){const question = this.getCurrentQuestion();if (!question) return null;const isCorrect = this.checkAnswer(question,answer);this.answers.push({question: question,userAnswer: answer,correct: isCorrect });if (isCorrect){this.score++} return{correct: isCorrect,correctAnswer: question.correctAnswer,explanation: question.explanation }} checkAnswer(question,userAnswer){if (question.type === 'multiple-choice'){return userAnswer === question.correctAnswer} else if (question.type === 'multiple-select'){const userAnswers = Array.isArray(userAnswer) ? userAnswer : [userAnswer];const correctAnswers = Array.isArray(question.correctAnswer) ? question.correctAnswer : [question.correctAnswer];return JSON.stringify(userAnswers.sort()) === JSON.stringify(correctAnswers.sort())} else if (question.type === 'true-false'){return userAnswer === question.correctAnswer} else if (question.type === 'fill-blank'){return userAnswer.toString().toLowerCase().trim() === question.correctAnswer.toString().toLowerCase().trim()} return false} nextQuestion(){if (!this.currentQuiz) return false;if (this.currentQuestionIndex < this.currentQuiz.questions.length - 1){this.currentQuestionIndex++;if (this.quizSettings.timedMode && this.quizSettings.timePerQuestion){this.timeRemaining = this.quizSettings.timePerQuestion;this.startTimer()} return true} this.stopTimer();return false} endQuiz(){this.stopTimer()} getResults(){if (!this.currentQuiz) return null;const totalQuestions = this.currentQuiz.questions.length;const percentage = Math.round((this.score / totalQuestions) * 100);const passed = percentage >= this.currentQuiz.passingScore;return{score: this.score,totalQuestions: totalQuestions,percentage: percentage,passed: passed,answers: this.answers,passingScore: this.currentQuiz.passingScore,hintsUsed: this.hintsUsed,timeSpent: this.quizSettings.timedMode ? (this.quizSettings.totalTime || this.quizSettings.timePerQuestion || 0) - this.timeRemaining : null }} saveResults(topicId,results){if (!this.progress[topicId]){this.progress[topicId] ={attempts: 0,bestScore: 0,lastAttempt: null,completed: false }} this.progress[topicId].attempts++;this.progress[topicId].lastAttempt = new Date().toISOString();if (results.percentage > this.progress[topicId].bestScore){this.progress[topicId].bestScore = results.percentage} if (results.passed){this.progress[topicId].completed = true} this.saveToStorage()} getProgress(topicId){return this.progress[topicId] ||{attempts: 0,bestScore: 0,lastAttempt: null,completed: false }} getOverallProgress(){const totalTopics = Object.keys(this.quizzes).length;const completedTopics = Object.values(this.progress) .filter(p => p.completed).length;const totalAttempts = Object.values(this.progress) .reduce((sum,p) => sum + p.attempts,0);const averageScore = Object.values(this.progress) .filter(p => p.bestScore > 0) .reduce((sum,p,_,arr) => sum + p.bestScore / arr.length,0);return{totalTopics: totalTopics,completedTopics: completedTopics,completionPercentage: totalTopics > 0 ? Math.round((completedTopics / totalTopics) * 100) : 0,totalAttempts: totalAttempts,averageScore: Math.round(averageScore) }} loadProgress(){try{const data = localStorage.getItem(this.storageKey);return data ? JSON.parse(data) :{}} catch (e){console.error('Error loading progress:',e);return{}} } saveToStorage(){try{localStorage.setItem(this.storageKey,JSON.stringify(this.progress))} catch (e){console.error('Error saving progress:',e)} } resetProgress(topicId){delete this.progress[topicId];this.saveToStorage()} resetAllProgress(){this.progress ={};this.saveToStorage()} } const chemieQuiz = new QuizSystem({storageKey: 'chemie-lernen-quiz-progress' });if (typeof module !== 'undefined' && module.exports){module.exports ={QuizSystem,chemieQuiz }} 