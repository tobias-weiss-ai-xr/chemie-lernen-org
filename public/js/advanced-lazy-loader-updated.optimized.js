class AdvancedLazyLoader{constructor(){this.loadedImages = new Set();this.loadingImages = new Set();this.observerOptions ={root: null,rootMargin: '50px',threshold: 0.1,};this.performanceMetrics ={totalImages: 0,loadedImages: 0,savedBandwidth: 0,averageLoadTime: 0,};this.init()} init(){this.setupImageObserver();this.setupIframeObserver();this.setupVideoObserver();this.setupResourceHints();imageObserver.unobserve(entry.target)} })},this.observerOptions);document.querySelectorAll('img[data-src]').forEach((img) =>{this.performanceMetrics.totalImages++;imageObserver.observe(img)})} loadImage(img){if (this.loadingImages.has(img)) return;this.loadingImages.add(img);const startTime = performance.now();const tempImg = new Image();tempImg.onload = () =>{const loadTime = performance.now() - startTime;img.src = img.dataset.src;img.classList.add('loaded');img.classList.remove('lazy-loading');this.updatePerformanceMetrics(loadTime,img.dataset.src);this.loadingImages.delete(img);this.loadedImages.add(img);tempImg.onerror = () =>{img.classList.add('error');img.classList.remove('lazy-loading');this.loadingImages.delete(img);console.error(`❌ Failed to load image: ${img.dataset.src}`)};tempImg.src = img.dataset.src} setupIframeObserver(){const iframeObserver = new IntersectionObserver((entries) =>{entries.forEach((entry) =>{if (entry.isIntersecting){this.loadIframe(entry.target);iframeObserver.unobserve(entry.target)} })},this.observerOptions);document.querySelectorAll('iframe[data-src]').forEach((iframe) =>{iframeObserver.observe(iframe)})} loadIframe(iframe){if (iframe.dataset.src){iframe.src = iframe.dataset.src;iframe.classList.add('loaded');videoObserver.unobserve(entry.target)} })},this.observerOptions);document.querySelectorAll('video[data-poster]').forEach((video) =>{videoObserver.observe(video)})} loadVideo(video){if (video.dataset.poster){video.poster = video.dataset.poster;video.classList.add('loaded');this.setupPrefetchHints();this.setupDNSPrefetch()} setupPreloadHints(){const criticalResources = [ '/js/chemistry-calculator-framework.optimized.js','/css/custom.css','/css/green-theme.css',];criticalResources.forEach((resource) =>{const link = document.createElement('link');link.rel = 'preload';link.href = resource;if (resource.endsWith('.js')){link.as = 'script'} else if (resource.endsWith('.css')){link.as = 'style'} document.head.appendChild(link)})} setupPrefetchHints(){const likelyResources = [ '/js/ph-rechner-framework.optimized.js','/js/molekuel-studio.optimized.js','/js/perioden-system-der-elemente.optimized.js',];likelyResources.forEach((resource) =>{const link = document.createElement('link');link.rel = 'prefetch';link.href = resource;document.head.appendChild(link)})} setupDNSPrefetch(){const externalDomains = ['cdn.jsdelivr.net','code.jquery.com','maxcdn.bootstrapcdn.com'];externalDomains.forEach((domain) =>{const link = document.createElement('link');link.rel = 'dns-prefetch';link.href = ` document.head.appendChild(link)})} updatePerformanceMetrics(loadTime,imageSrc){this.performanceMetrics.loadedImages++;this.performanceMetrics.averageLoadTime = (this.performanceMetrics.averageLoadTime * (this.performanceMetrics.loadedImages - 1) + loadTime) / this.performanceMetrics.loadedImages;const estimatedSize = this.estimateImageSize(imageSrc);this.performanceMetrics.savedBandwidth += estimatedSize * 0.3} estimateImageSize(imageSrc){if (imageSrc.includes('.webp')){return 50000} else if (imageSrc.includes('.jpg') || imageSrc.includes('.jpeg')){return 80000} else if (imageSrc.includes('.png')){return 120000} return 100000} getPerformanceMetrics(){return{...this.performanceMetrics,loadRate: this.performanceMetrics.loadedImages / this.performanceMetrics.totalImages || 0,estimatedBandwidthSaved: this.formatBytes(this.performanceMetrics.savedBandwidth),}} formatBytes(bytes){if (bytes === 0) return '0 Bytes';const k = 1024;const sizes = ['Bytes','KB','MB'];const i = Math.floor(Math.log(bytes) / Math.log(k));return Math.round((bytes / Math.pow(k,i)) * 100) / 100 + ' ' + sizes[i]} generateLazyLoadHTML(){return ` <style> .lazy-loading{opacity: 0;transition: opacity 0.3s ease-in-out} .lazy-loaded{opacity: 1} .lazy-placeholder{background: linear-gradient(90deg,#f0f0f0 25%,#e0e0e0 50%,#f0f0f0 75%);background-size: 200% 100%;animation: loading 1.5s infinite} @keyframes loading{0%{background-position: 200% 0} 100%{background-position: -200% 0} } .loaded{animation: fadeIn 0.5s ease-in-out} @keyframes fadeIn{from{opacity: 0;transform: translateY(20px)} to{opacity: 1;transform: translateY(0)} } </style> <script> window.AdvancedLazyLoader = new AdvancedLazyLoader();function convertImagesToLazy(){document.querySelectorAll('img:not([data-src])').forEach(img =>{if (img.src && !img.src.startsWith('data:')){img.dataset.src = img.src;img.src = '';img.classList.add('lazy-loading');const placeholder = document.createElement('div');placeholder.className = 'lazy-placeholder';placeholder.style.width = img.width + 'px';placeholder.style.height = img.height + 'px';img.parentNode.insertBefore(placeholder,img);img.style.display = 'none';setTimeout(() =>{placeholder.remove();img.style.display = '';window.AdvancedLazyLoader.setupImageObserver()},100)} })} if (document.readyState === 'loading'){document.addEventListener('DOMContentLoaded',convertImagesToLazy)} else{convertImagesToLazy()} </script> `} } if (typeof window !== 'undefined'){window.AdvancedLazyLoader = AdvancedLazyLoader;if (document.readyState === 'loading'){document.addEventListener('DOMContentLoaded',() =>{new AdvancedLazyLoader()})} else{new AdvancedLazyLoader()} } if (typeof module !== 'undefined' && module.exports){module.exports = AdvancedLazyLoader} 