 let titrationChart = null;function calculatePH(analyteType,analyteConc,analyteVol,titrantType,titrantConc,titrantVol,pka = 0,pkb = 0){const kw = 1e-14;const Va = analyteVol / 1000;const Vb = titrantVol / 1000;const Vtot = Va + Vb;const molesAcid = analyteConc * Va;const molesBase = titrantConc * Vb;let ph = 7.0;if (analyteType === 'strong-acid' && titrantType === 'strong-base'){const excessH = molesAcid - molesBase;if (excessH > 0){ph = -Math.log10(excessH / Vtot)} else if (excessH < 0){const excessOH = -excessH;const pOH = -Math.log10(excessOH / Vtot);ph = 14 - pOH} else{ph = 7.0} } else if (analyteType === 'weak-acid' && titrantType === 'strong-base'){const ka = Math.pow(10,-pka);if (molesBase === 0){const ha = analyteConc;const h = Math.sqrt(ka * ha);ph = -Math.log10(h)} else if (molesBase < molesAcid){const haRemaining = molesAcid - molesBase;const aFormed = molesBase;ph = pka + Math.log10(aFormed / haRemaining)} else if (molesBase === molesAcid){const aConc = molesAcid / Vtot;const kb = kw / ka;const oh = Math.sqrt(kb * aConc);const pOH = -Math.log10(oh);ph = 14 - pOH} else{const excessOH = molesBase - molesAcid;const pOH = -Math.log10(excessOH / Vtot);ph = 14 - pOH} } else if (analyteType === 'strong-acid' && titrantType === 'weak-base'){const kb = Math.pow(10,-pkb);const ka = kw / kb;if (molesBase < molesAcid){const excessH = molesAcid - molesBase;ph = -Math.log10(excessH / Vtot)} else if (molesBase === molesAcid){const bhConc = molesBase / Vtot;const h = Math.sqrt(ka * bhConc);ph = -Math.log10(h)} else{const excessB = molesBase - molesAcid;const bConc = excessB / Vtot;const oh = Math.sqrt(kb * bConc);const pOH = -Math.log10(oh);ph = 14 - pOH} } else if (analyteType === 'weak-acid' && titrantType === 'weak-base'){const ka = Math.pow(10,-pka);const kb = Math.pow(10,-pkb);if (molesBase === 0){const ha = analyteConc;const h = Math.sqrt(ka * ha);ph = -Math.log10(h)} else if (molesBase < molesAcid){const haRemaining = molesAcid - molesBase;const aFormed = molesBase;ph = pka + Math.log10(aFormed / haRemaining)} else if (molesBase === molesAcid){ph = 7 + (pka - pkb) / 2} else{const excessB = molesBase - molesAcid;const bConc = excessB / Vtot;const oh = Math.sqrt(kb * bConc);const pOH = -Math.log10(oh);ph = 14 - pOH} } return Math.max(0,Math.min(14,ph))} function runTitration(){const analyteType = document.getElementById('analyte-type').value;const analyteConc = parseFloat(document.getElementById('analyte-concentration').value);const analyteVol = parseFloat(document.getElementById('analyte-volume').value);const analytepKa = parseFloat(document.getElementById('analyte-pka').value);const titrantType = document.getElementById('titrant-type').value;const titrantConc = parseFloat(document.getElementById('titrant-concentration').value);const titrationStep = parseFloat(document.getElementById('titration-step').value);const titrantpKb = parseFloat(document.getElementById('titrant-pkb').value);if (!analyteConc || !analyteVol || !titrantConc || !titrationStep){showError('Bitte füllen Sie alle Pflichtfelder aus.');return} if (analyteConc <= 0 || analyteVol <= 0 || titrantConc <= 0 || titrationStep <= 0){showError('Alle Werte müssen positiv sein.');return} if (analyteType === 'weak-acid' && !analytepKa){showError('Bitte geben Sie den pKa-Wert für die schwache Säure ein.');return} if (titrantType === 'weak-base' && !titrantpKb){showError('Bitte geben Sie den pKb-Wert für die schwache Base ein.');return} try{const molesAnalyte = analyteConc * (analyteVol / 1000);const equivVolume = (molesAnalyte / titrantConc) * 1000;const maxVolume = equivVolume * 2;const dataPoints = [];for (let v = 0;v <= maxVolume;v += titrationStep){const ph = calculatePH(analyteType,analyteConc,analyteVol,titrantType,titrantConc,v,analytepKa,titrantpKb);dataPoints.push({volume: v,ph: ph })} const equivPH = calculatePH(analyteType,analyteConc,analyteVol,titrantType,titrantConc,equivVolume,analytepKa,titrantpKb);if (!dataPoints.some(p => Math.abs(p.volume - equivVolume) < titrationStep / 2)){dataPoints.push({volume: equivVolume,ph: equivPH })} dataPoints.sort((a,b) => a.volume - b.volume);displayResults(analyteType,titrantType,analyteConc,analyteVol,titrantConc,equivVolume,equivPH,dataPoints,analytepKa,titrantpKb)} catch (error){showError(error.message)} } function displayResults(analyteType,titrantType,analyteConc,analyteVol,titrantConc,equivVolume,equivPH,dataPoints,pka,pkb){const typeNames ={'strong-acid': 'Starke Säure','weak-acid': 'Schwache Säure','strong-base': 'Starke Base','weak-base': 'Schwache Base' };const resultsHtml = ` <div class="data-row"> <span class="data-label">Analysenlösung:</span> <span class="data-value">${typeNames[analyteType]} (${analyteConc} M,${analyteVol} mL)</span> </div> <div class="data-row"> <span class="data-label">Titrationslösung:</span> <span class="data-value">${typeNames[titrantType]} (${titrantConc} M)</span> </div> <div class="data-row"> <span class="data-label">Äquivalenzvolumen:</span> <span class="data-value value-important">${equivVolume.toFixed(2)} mL</span> </div> ${pka > 0 ? ` <div class="data-row"> <span class="data-label">pKa / pKb:</span> <span class="data-value">${pka.toFixed(2)} / ${pkb.toFixed(2)}</span> </div> ` : ''} <div class="data-row"> <span class="data-label">Empfohlener Indikator:</span> <span class="data-value">${getRecommendedIndicator(equivPH)}</span> </div> `;document.getElementById('titration-results').innerHTML = resultsHtml;const equivHtml = ` <div class="data-row"> <span class="data-label">Volumen am Äq.-Punkt:</span> <span class="data-value value-important">${equivVolume.toFixed(2)} mL</span> </div> <div class="data-row"> <span class="data-label">pH am Äq.-Punkt:</span> <span class="data-value value-important">${equivPH.toFixed(2)}</span> </div> <div class="data-row"> <span class="data-label">pH-Sprung:</span> <span class="data-value">${calculatePHJump(dataPoints,equivVolume).toFixed(2)} pH-Einheiten</span> </div> <hr> <div class="data-row"> <span class="data-label">Pufferbereich:</span> <span class="data-value">${pka > 0 ? `${(pka - 1).toFixed(1)} - ${(pka + 1).toFixed(1)}` : 'Nicht anwendbar'}</span> </div> `;document.getElementById('equivalence-point').innerHTML = equivHtml;drawTitrationChart(dataPoints,equivVolume,equivPH);displayDataPoints(dataPoints);showResults()} function calculatePHJump(dataPoints,equivVolume){const equivIndex = dataPoints.findIndex(p => Math.abs(p.volume - equivVolume) < 0.5);if (equivIndex < 2 || equivIndex >= dataPoints.length - 2){return 0} const phBefore = dataPoints[equivIndex - 2].ph;const phAfter = dataPoints[equivIndex + 2].ph;return phAfter - phBefore} function getRecommendedIndicator(ph){if (ph < 5){return 'Methylorange (3.1-4.4) oder Methylrot (4.4-6.2)'} else if (ph < 7){return 'Methylrot (4.4-6.2) oder Bromthymolblau (6.0-7.6)'} else if (ph < 9){return 'Bromthymolblau (6.0-7.6) oder Phenolphthalein (8.2-10.0)'} else{return 'Phenolphthalein (8.2-10.0) oder Thymolphthalein (9.3-10.5)'} } function drawTitrationChart(dataPoints,equivVolume,equivPH){const ctx = document.getElementById('titration-chart');if (!ctx) return;if (titrationChart){titrationChart.destroy()} const canvas = document.createElement('canvas');canvas.width = 600;canvas.height = 400;ctx.innerHTML = '';ctx.appendChild(canvas);const chartCtx = canvas.getContext('2d');titrationChart = new Chart(chartCtx,{type: 'line',data:{labels: dataPoints.map(p => p.volume.toFixed(1)),datasets: [{label: 'pH-Wert',data: dataPoints.map(p => p.ph),borderColor: 'rgb(75,192,192)',backgroundColor: 'rgba(75,192,192,0.1)',borderWidth: 2,pointRadius: dataPoints.map(p => Math.abs(p.volume - equivVolume) < 0.5 ? 6 : 2),pointBackgroundColor: dataPoints.map(p => Math.abs(p.volume - equivVolume) < 0.5 ? 'red' : 'rgb(75,192,192)'),tension: 0.4,fill: true },{label: 'Äquivalenzpunkt',data: dataPoints.map(p => Math.abs(p.volume - equivVolume) < 0.5 ? p.ph : null),borderColor: 'red',backgroundColor: 'red',pointRadius: 8,pointStyle: 'triangle',showLine: false }] },options:{responsive: true,maintainAspectRatio: true,plugins:{title:{display: true,text: `Titrationkurve (Äquivalenzpunkt: ${equivVolume.toFixed(2)} mL,pH: ${equivPH.toFixed(2)})`,font:{size: 16 } },legend:{display: true,position: 'top' },tooltip:{callbacks:{label: function(context){return `pH: ${context.parsed.y.toFixed(2)}`} } } },scales:{x:{display: true,title:{display: true,text: 'Titrationsvolumen (mL)' },ticks:{maxTicksLimit: 15 } },y:{display: true,title:{display: true,text: 'pH-Wert' },min: 0,max: 14 } },interaction:{intersect: false,mode: 'index' } } })} function displayDataPoints(dataPoints){const maxRows = Math.min(dataPoints.length,20);const stepSize = Math.ceil(dataPoints.length / maxRows);let html = '<div class="table-responsive"><table class="table table-striped table-hover table-sm">';html += '<thead><tr><th>Volumen (mL)</th><th>pH-Wert</th></tr></thead><tbody>';for (let i = 0;i < dataPoints.length;i += stepSize){const point = dataPoints[i];html += `<tr><td>${point.volume.toFixed(2)}</td><td>${point.ph.toFixed(3)}</td></tr>`} html += '</tbody></table></div>';document.getElementById('data-points').innerHTML = html} function showResults(){document.getElementById('results-section').style.display = 'block';document.getElementById('error-section').style.display = 'none';document.getElementById('results-section').scrollIntoView({behavior: 'smooth',block: 'start' })} function showError(message){document.getElementById('error-message').textContent = message;document.getElementById('error-section').style.display = 'block';document.getElementById('results-section').style.display = 'none'} document.addEventListener('DOMContentLoaded',function(){document.getElementById('analyte-type').addEventListener('change',function(){const pkaRow = document.getElementById('analyte-pka-row');if (this.value === 'weak-acid'){pkaRow.style.display = 'block'} else{pkaRow.style.display = 'none'} });document.getElementById('titrant-type').addEventListener('change',function(){const pkbRow = document.getElementById('titrant-pkb-row');if (this.value === 'weak-base'){pkbRow.style.display = 'block'} else{pkbRow.style.display = 'none'} });document.querySelectorAll('.example-btn').forEach(button =>{button.addEventListener('click',function(){document.getElementById('analyte-type').value = this.dataset.analyte;document.getElementById('titrant-type').value = this.dataset.titrant;if (this.dataset.analyte === 'weak-acid'){document.getElementById('analyte-pka-row').style.display = 'block';document.getElementById('analyte-pka').value = this.dataset.pka || 4.76} else{document.getElementById('analyte-pka-row').style.display = 'none'} if (this.dataset.titrant === 'weak-base'){document.getElementById('titrant-pkb-row').style.display = 'block';document.getElementById('titrant-pkb').value = this.dataset.pkb || 4.75} else{document.getElementById('titrant-pkb-row').style.display = 'none'} document.getElementById('analyte-concentration').value = this.dataset.acidConc || 0.1;document.getElementById('titrant-concentration').value = this.dataset.baseConc || 0.1})});const inputs = document.querySelectorAll('.form-control');inputs.forEach(input =>{input.addEventListener('keypress',function(e){if (e.key === 'Enter'){runTitration()} })})});